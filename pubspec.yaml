name: pabtugasuas
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.13.1
  firebase_database: ^11.3.6
  firebase_auth: 5.5.4
  google_fonts: ^6.2.1
  google_sign_in: ^6.2.2
  intl: ^0.20.2
  foundation: ^0.0.5
  cloud_firestore: ^5.6.8


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/preloved_logo.png  # Pastikan ini ada dan sesuai dengan path gambar
